(() => {
    'use strict';

    angular.module('localModulo').component('localCard', {
        templateUrl: 'view/component/local/local-card.html',
        bindings: {
            local: '='
        },
        controller: [function () {
        }],
    });
})();
